@model IEnumerable<WebApplication1.ClientApp.Data.Stage3>

@{
    ViewData["Title"] = "List";
}

<h2>List</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Stage3_ID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Stage_name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.helptext_ID)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Stage3_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stage_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.helptext_ID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Stage3_ID}) |
                @Html.ActionLink("Details", "Details", new { id = item.Stage3_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Stage3_ID })
            </td>
        </tr>
}
    </tbody>
</table>
