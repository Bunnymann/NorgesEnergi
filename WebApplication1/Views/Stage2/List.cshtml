@model IEnumerable<WebApplication1.ClientApp.Model.Stage2>

@{
    ViewData["Title"] = "List";
}

<h2>List</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.stage2_ID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.stage2_name)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.stage2_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.stage2_name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.stage2_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.stage2_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.stage2_ID })
            </td>
        </tr>
}
    </tbody>
</table>
