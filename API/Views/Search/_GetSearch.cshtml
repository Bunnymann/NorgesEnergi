@using (Html.BeginForm("Index", "Search", FormMethod.Get))
{
    @Html.TextBox("tags") <input type="submit"
                                 name="submit"
                                 value="Search" />
}