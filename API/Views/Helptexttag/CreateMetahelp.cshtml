@model API.Models.HelpTagsViewModel
@{
    ViewBag.Title = "CreateMetahelp";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>CreateMetahelp</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <h4>Stages</h4>
        @Html.ValidationSummary(true)

        <div class="form-group">
            <label class="control-label col-md-2" for="helptext_ID">Helptext header</label>
            <div class="col-md-10">
                @Html.DropDownList("helptext_ID", String.Empty)
                @Html.ValidationMessageFor(model => model.helptext_ID)
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2" for="metatag_ID">Metatag</label>
            <div class="col-md-10">
                @Html.DropDownList("metatag_ID", String.Empty)
                @Html.ValidationMessageFor(model => model.tag_ID)
            </div>
        </div>
    </div>

<div class="form-group">
    <div class="col-md-offset-2 col-md-10">
        <input type="submit" value="Create" class="brn btn-default" />
    </div>
</div>
}