@model IEnumerable<API.Models.users>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "createUser")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.loginname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.loginpassword)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.stage1_ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.loginname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.loginpassword)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.stage1_ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
